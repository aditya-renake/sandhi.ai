# ML / Computer Vision dependencies
# NOT deployed to Vercel (exceeds 250MB bundle limit)
# Use locally or deploy CV endpoints to a separate service (e.g. Railway, Render, Modal)

-r requirements.txt

opencv-python-headless>=4.9.0
scipy>=1.12.0
torch>=2.0.0
torchvision>=0.15.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
